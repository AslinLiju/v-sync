// src/components/SyncButton.js
import React from 'react';
import './SyncButton.css';

const SyncButton = () => {
    return <button className="sync-button">SYNC</button>;
};

export default SyncButton;
